# 本版本为纯前端应用：API Key 在应用右上角 ⚙️ 设置里填写，保存在浏览器本地。
# 不需要任何服务端环境变量。
#
# 部署到子路径（如 GitHub Pages 的 /repo-name/）时，构建前设置：
# BASE_PATH=/repo-name
#
# 如需更换模型名（默认 deepseek-chat / deepseek-flash）：
# NEXT_PUBLIC_DEEPSEEK_MODEL=deepseek-chat
# NEXT_PUBLIC_DEEPSEEK_VISION_MODEL=deepseek-flash
