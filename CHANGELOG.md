# 更新日志

本文件记录「今天吃什么呀」的所有重要变更。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

版本号的**单一事实来源是 `package.json` 的 `version`**，本文件与
`app/lib/changelog.ts`（App 内展示用）需与之保持一致。

递增规则：

| 变更类型 | 递增 | 例子 |
|---|---|---|
| 不兼容改动（数据格式、交互大改） | MAJOR | 1.0.0 → 2.0.0 |
| 新增功能 | MINOR | 1.0.0 → 1.1.0 |
| 修 bug、样式微调 | PATCH | 1.0.0 → 1.0.1 |

> `1.0.0` 是本项目正式版本化的起点。此前的改动不再追溯编号——那只会是推测。

---

## [1.1.0] - 2026-09-14

### 新增

- **补录打卡**：漏记的日期可以补回来，最多往前 30 天。
  两个入口：①「今日打卡」卡片右上角的日期切换（‹ 日期 ›）；
  ②「本周打卡」里点任意可补的日期格子直接跳到那天。
  补录复用全部输入控件（喝水/步数/睡眠/状态），进度条与「还差 X」提示照常适用。
- **连续天数整体重算**：补上中间断掉的那天会把前后两段连起来
  （例如已有 1、2 和 4、5，补上 3 后 5 那天的连续值从 2 变为 5）。
  因此「🎬 回看庆祝」显示的天数与徽章也随补录同步更新。
- 补录达标只给一行轻提示（如「已补录 09-12，当前连续 3 天」），
  不弹庆祝动画——补录属于事后记账，弹庆祝会显得错位。

### 说明

- 奖励数据仍只存本地，补录同样会纳入备份导出。
- 补录不能填未来日期，也不能超过 30 天前。

---

## [1.0.0] - 2026-09-14

首个正式版本。相对上游项目（[FishBone0v0/AI-recipe-recommendation-system](https://github.com/FishBone0v0/AI-recipe-recommendation-system)）新增：

### 新增

- **健康小屋**：录入性别、年龄、身高体重、日常活动量、健康目标、忌口过敏与身体状况；
  自动计算 BMR / TDEE（Mifflin-St Jeor 公式）、每日热量参考、喝水目标（约 35ml/kg）、
  步数目标（按活动量）、BMI。
- **每日打卡**：喝水（按「杯」录入）、步数（档位快选 / 滑块 / 直接输入）、睡眠、今日状态；
  进度条带「还差 X 杯 / 还差 X 步（约 X km）」文案；首页有今日进度卡片。
- **连续打卡奖励**：喝水与步数双达标即计入连续天数；达标瞬间弹窗庆祝
  （spring 入场 + 数字滚动 + 彩带）；六枚里程碑徽章（3/7/14/30/60/100 天）；
  达成记录可随时「回看庆祝」。今天未达标时连续天数从昨天起算，避免白天打开显示 0 天。
- **食堂/外卖菜单库**：截图导入菜单，AI 自动识别**店铺名**、菜名、价格并补齐口味与忌口标签；
  一次可导入多个店铺的截图并自动归属；支持手动添加、修改（商家/菜名/类别/价格/标签）、
  删除、按商家分组浏览、恢复示例库；同店同名自动去重。
- **推荐接入健康数据**：健康档案与当日打卡会一并交给 AI，自己做推荐、外卖推荐、
  本周分析都严格避开忌口/过敏，并贴合热量参考与健康目标。
- **快捷记一餐**：已吃过饭时不必走推荐流程，选定食材后可让 AI 起名或自己填，直接记账。
- **数据备份**：导出为文件 / 复制内容 / 合并导入 / 覆盖导入 / 清空本机数据；
  用于换机迁移，或把自己的配置（含 API Key）一次导给家人。
- **设置页**：API Key、数据备份、版本三个分区；版本区显示 App 版本与构建时间，
  并提供「强制刷新到最新版」。

### 变更

- **纯前端化**：移除全部服务端 API 路由，改为浏览器直连 DeepSeek
  （文本用 `deepseek-chat`，截图识别用 V4.1 Flash 的视觉能力 `deepseek-flash`）。
  因此可完整静态导出，无服务器、零成本，数据只存本地。
- **手机独立使用**：支持「添加到主屏幕」当 App 用；iOS 侧做了安全区适配、
  4 种机型启动图、`storage.persist()` 申请持久存储、安装引导横幅。
- **更新机制**：Service Worker 缓存版本化、HTML 强制验证（绕过 HTTP 缓存）、
  `updateViaCache: "none"`、回到 App 自动检查更新、发现新版本时页面顶部提示。
  更新不影响本地数据。

### 修复

- 修复子路径部署下站内跳转 404（原生 `<a>` 不会自动加 basePath）。
- 修复健康档案的年龄/身高/体重**删不掉、总留个 0**（受控数字输入把空串读成 0）。
- 修复截图导入时**不识别店铺名**，导致所有菜都归到「学校食堂」。
- 修复 Service Worker 的 `cache.addAll` 静默失败（任一 404 会让整个离线缓存为空）。
- 修复 iOS 主屏 App 更新后看不到新版、以及点「立即更新」无反应的问题。
- 修复「发现新版本」横幅在首次安装时误报。

### 部署

- 新增 `部署.bat`：一键完成构建（含子路径）、注入版本号、发布到 GitHub Pages。
- 新增 `scripts/verify-subpath.py`：解析页面里的真实链接逐个访问，
  等价于把每个按钮点一遍，用于补上「只测路由地址测不出点击 404」的漏洞。

### 说明

- 本项目是上游项目的衍生版本。上游**未附 LICENSE、未声明开源协议**（默认保留所有权利），
  故本衍生版仅供个人自用，不应公开分发。详见 `README.md` 顶部声明。
