# 更新日志

本文件记录「今天吃什么呀」的所有重要变更。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

版本号的**单一事实来源是 `package.json` 的 `version`**，本文件与
`app/lib/changelog.ts`（App 内展示用）需与之保持一致。

递增规则（**偏向克制：能用 PATCH 就别用 MINOR**）：

| 变更类型 | 递增 | 例子 |
|---|---|---|
| 不兼容改动（数据格式、交互大改） | MAJOR | 1.0.0 → 2.0.0 |
| **成块的新功能**（新增一个完整模块/页面/能力） | MINOR | 1.0.0 → 1.1.0 |
| **其余一切**：修 bug、样式/文案调整、加一个按钮或交互优化、小功能增强 | PATCH | 1.0.0 → 1.0.1 |
| 只改文档 / 注释 / 构建脚本 | 不升版本 | — |

> 判断依据是"用户能感知到的变化有多成块"，不是"改了多少行代码"。
> 拿不准时选小的那个（PATCH）。
>
> `1.0.0` 是本项目正式版本化的起点。此前的改动不再追溯编号——那只会是推测。

---

## [1.5.3] - 2026-09-15

### 新增

- **体重补录**：体重卡新增「记录日期」，默认今天，可往前切（最多 30 天）——
  漏记的那天点一下 ‹ 就补上；历史列表点任意一行也能直接切到那天改。
  - 补录过去的那天**只写历史，不动健康档案体重**：否则热量（BMR/TDEE）与
    喝水目标（35ml/kg）会被一条旧数据悄悄改掉。只有记录/更新**今天**才同步档案并重算目标
    （判据不能写成"比已有记录新"——用户上次记录是三天前时，补录昨天也确实"更新"，
    但那仍是历史补录，不是今天的体重）。
  - 补录的起始值取「该日期之前最近的一条」（没有则用最新一条 → 档案体重 → 60 兜底），
    不必从 60kg 一路点上去。
- **喝水改成「我的杯子」**：一杯多少由自己定 —— 快选 150/200/250/300/400/500ml 或自定义
  （100–1000ml，默认仍是 250ml，记住在本机）；快捷按钮跟着变（+1 杯 / +½ 杯 / +500ml / −1 杯）。
  完全不按杯算也行：直接填「＋___ ml」记一笔。

### 变更

- 喝水进度文案改为 ml 在前：**「还差 301ml（约 2 杯）」**。杯数向上取整，200ml 的杯子
  "2 杯"其实是 400ml，写成「还差 2 杯（约 301ml）」会自相矛盾。首页「今日进度」的
  「还差约 N 杯」也改用你自己的杯容量（原先是写死的 250ml）。
- 补录体重时「较上次 / 近 7 天」仍按最新一条记录计算（卡片上有一行说明），
  避免与正在补录的那天混淆。

### 说明

- **ml 仍是唯一的存储单位**（`DailyCheckin.waterMl`）：换杯子只影响换算与快捷按钮，
  不改动任何已记录的水量，因此**不需要数据迁移**。
- 新增偏好键 `recipe.prefs.v1`：在 `recipe.` 前缀下，自动随备份导出/导入/清空，
  导入预览里显示为「偏好设置」；没有这个键时一律按 250ml 处理。
- 体重补录窗口与打卡补录共用 `BACKFILL_DAYS = 30`（`app/lib/date.ts`），两处能补到哪天一致。

---

## [1.5.2] - 2026-09-15

### 变更

- **放开切段数量上限**（10 → 20 段，一次最多 24 段）：
  遇到极端长图（如 1080×26092 的整页外卖菜单）时，原先会因段数上限而
  **整体缩小到 75%**，牺牲清晰度。现在保持原分辨率切段
  （该尺寸约 18 段），菜名与价格更清楚。
  代价是图片数量变多（每段约 384 token，请求体仍在 48MiB 内）。

---

## [1.5.1] - 2026-09-14

### 修复

- **长截图导入失败**（报 `unsupported image`）。根因有两条：
  1. 图片预处理 `fileToDataUrl` **只限制宽度 1600、不限高度**。手机截图宽约 1080 < 1600，
     于是不做任何缩放、长图原样发出；而接口要求**单边最长 8192 像素**，
     一张 1080×12000 的菜单长图必然被拒。
  2. 即使不超限也没用：接口会在进模型前把图片等比缩到 **约 1300×1300 等效**，
     长图被压扁后文字只剩三分之一高，菜名会糊掉、识别大面积漏菜。
     （所以"把图缩小"是错的修法。）
- **改为按段切图**：新增 `app/lib/image.ts`，把长图按「总像素 ≤ 约 250 万 且 单边 ≤ 4096」
  切成多段（相邻段重叠 80px，避免把菜名拦腰切断），每段单独作为一张图发送。
  每段都落在自动缩放阈值内，**文字保持原分辨率**。
- **修掉一个静默失败**：canvas 面积越限时浏览器会返回空的 `"data:,"`，
  旧代码不校验，会把空图发给接口 —— 现在会检测并给出中文提示。
- **防止切段后商家归属错乱**：只有第一段能看到店名，其余段模型返回空 merchant，
  旧逻辑会兜底成"学校食堂"、把这家店的菜归错。现在空 merchant 会**沿用上一组的店名**，
  并在 prompt 里说明"连续多段视为同一家店"。
- **图片报错中文化**：把接口的英文原文（`unsupported image` 等）
  映射成可照做的提示（如"请把这张图截成 2~3 段分别上传"）。

### 说明

- 一次最多接受 **10 段**图片（长图切段会放大数量，故比原来的 5 张宽松）；
  检测到长图时界面会提示"已自动切成 N 段"。
- 普通截图（如 1080×2400）仍然只产生 1 段，行为与之前一致。
- 仍不使用 Files API：切段后 base64 直传已足够（单段约 1~2MB，远低于 32MiB 单图上限）。

---

## [1.5.0] - 2026-09-14

### 新增

- **商家改名**：菜单库里每个商家右侧新增「✏️ 改名」，把该商家名下**所有菜品**
  一起换到新店名。点击后**就地变成输入框**（不用 `window.prompt` ——
  它在 iOS 主屏 App 上不可靠），支持 Enter 保存 / Esc 取消。
- **改名即合并**：若新店名已存在，两家会合并，并按「店名 + 菜名」去重
  （保留先出现的那条），结果提示会如实说明合并了几道。
  这正好解决了「杨国福麻辣烫」与「杨国福麻辣烫(五道口店)」这类
  **店名写法不一致造成的重复商家**——以前只能一道道改。
- 改名同样会先存操作快照，可一键撤销。

### 说明

- 同名判定仍是**精确匹配**：改名只在店名完全相同时才合并。
- 改名输入框补了 `aria-label`，方便读屏与自动化测试定位。

---

## [1.4.0] - 2026-09-14

### 新增

- **一键删除整个商家**：菜单库里每个商家右侧有「🗑️ 删除这家（N 道）」，
  连同它名下所有菜品一起删掉；确认框里写清影响多少道菜。
- **导入时可覆盖**：
  - 「同名菜品用新数据覆盖」（**默认开启**）：同商家同菜名的记录改用本次导入的数据
    （价格/类别/标签更新），并**复用原有 id**，不会让正在编辑的弹窗错位。
    之前同名是"跳过"，会导致旧价格永远留在库里。
  - 「先清空整个菜单库再导入」（**默认关闭**，需二次确认）：只保留本次导入的内容。
- **操作快照 + 一键撤销**：删除商家、删除菜品、覆盖导入、清空库、恢复示例库
  之前都会自动存一份快照；菜单库顶部出现「↩︎ 可撤销：<操作说明>」，
  点一下即可完整恢复（单槽，只能撤销一次）。
- 菜单库顶部新增「🗑️ 清空」按钮，并在列表区给出操作结果反馈。

### 修复

- **修掉"把商家全删光后刷新页面，示例库会自动回来"**：
  原先 `seedTakeoutMockIfEmpty` 以"库为空"为条件重新灌入 29 条示例菜，
  导致删除看起来没生效。改为由一次性标记 `recipe.takeoutSeeded.v1` 控制，
  只在真正的首次运行时播种；老用户（已有数据但无标记）只会补写标记，
  不会被动覆盖数据。「恢复示例库」是唯一允许重新播种的入口。

### 说明

- 撤销快照存 `recipe.takeoutUndo.v1`（单槽，只保留最近一次），
  随备份导出，也会被「清空全部数据」一并清掉。
- 同名判定为**精确匹配**「商家 + 菜名」字符串；同一家店若店名写法不同
  （如带不带分店后缀），会被视为两家店。

---

## [1.3.0] - 2026-09-14

### 新增

- **运动记录**（健康小屋）：可记散步 / 跑步 / 爬山 / 徒步 / 骑行 / 游泳 / 球类 / 其他，
  每条可填时长、距离、备注（时长与距离至少填一个）；一天可记多条。
  - 本周概览（次数 / 时长 / 里程）与最近记录列表（可单条删除）
  - 与步数打卡**互相独立**，不做联动，避免重复计算
- **运动里程碑**：第一次运动 🌱 / 累计 10 次 🎯 / 累计 30 次 🏅 / 累计 50km 🏃 /
  累计 100km 🏔️ / 徒步·爬山 10 次 🥾。达成时弹出庆祝动画（复用打卡的彩带与入场），
  普通记录只给一行轻提示。

### 说明

- 运动数据存 `recipe.exercises.v1`，里程碑存 `recipe.exerciseAwards.v1`，
  均在 `recipe.` 前缀下，自动纳入备份导出/清空。
- 里程与次数里程碑与「连续打卡」徽章使用**互相独立的命名空间**，互不影响。

## [1.2.0] - 2026-09-14

### 新增

- **体重记录**（健康小屋）：每天一条，同一天重复记录覆盖。
  - 递增递减微调（±0.1 / ±0.5）或直接输入，一键「记录今天」
  - **趋势曲线**：最近 30 条记录的折线图（纯 SVG，不引图表库），
    带最高/最低参考线与首尾日期；不足两天时显示"记满两天就能看到趋势曲线"
  - **变化对比**：「较上次」与「近 7 天」两个差值，↓ 显示为蓝色（减重）、↑ 为琥珀色
  - **历史列表**：每条显示日期、体重与相对上一条的差值，可单条删除
- **同步档案体重**：记录体重后自动更新健康档案里的体重，
  喝水目标（约 35ml/kg）与热量目标（BMR/TDEE）随之重算。

### 说明

- 体重数据存 `recipe.weights.v1`，自动纳入备份导出/清空。
- 体重与打卡奖励互不影响（连续天数只看喝水与步数）。

---

## [1.1.0] - 2026-09-14

### 新增

- **补录打卡**：漏记的日期可以补回来，最多往前 30 天。
  两个入口：①「今日打卡」卡片右上角的日期切换（‹ 日期 ›）；
  ②「本周打卡」里点任意可补的日期格子直接跳到那天。
  补录复用全部输入控件（喝水/步数/睡眠/状态），进度条与「还差 X」提示照常适用。
- **连续天数整体重算**：补上中间断掉的那天会把前后两段连起来
  （例如已有 1、2 和 4、5，补上 3 后 5 那天的连续值从 2 变为 5）。
  因此「🎬 回看庆祝」显示的天数与徽章也随补录同步更新。
- 补录达标只给一行轻提示（如「已补录 09-12，当前连续 3 天」），
  不弹庆祝动画——补录属于事后记账，弹庆祝会显得错位。

### 说明

- 奖励数据仍只存本地，补录同样会纳入备份导出。
- 补录不能填未来日期，也不能超过 30 天前。

---

## [1.0.0] - 2026-09-14

首个正式版本。相对上游项目（[FishBone0v0/AI-recipe-recommendation-system](https://github.com/FishBone0v0/AI-recipe-recommendation-system)）新增：

### 新增

- **健康小屋**：录入性别、年龄、身高体重、日常活动量、健康目标、忌口过敏与身体状况；
  自动计算 BMR / TDEE（Mifflin-St Jeor 公式）、每日热量参考、喝水目标（约 35ml/kg）、
  步数目标（按活动量）、BMI。
- **每日打卡**：喝水（按「杯」录入）、步数（档位快选 / 滑块 / 直接输入）、睡眠、今日状态；
  进度条带「还差 X 杯 / 还差 X 步（约 X km）」文案；首页有今日进度卡片。
- **连续打卡奖励**：喝水与步数双达标即计入连续天数；达标瞬间弹窗庆祝
  （spring 入场 + 数字滚动 + 彩带）；六枚里程碑徽章（3/7/14/30/60/100 天）；
  达成记录可随时「回看庆祝」。今天未达标时连续天数从昨天起算，避免白天打开显示 0 天。
- **食堂/外卖菜单库**：截图导入菜单，AI 自动识别**店铺名**、菜名、价格并补齐口味与忌口标签；
  一次可导入多个店铺的截图并自动归属；支持手动添加、修改（商家/菜名/类别/价格/标签）、
  删除、按商家分组浏览、恢复示例库；同店同名自动去重。
- **推荐接入健康数据**：健康档案与当日打卡会一并交给 AI，自己做推荐、外卖推荐、
  本周分析都严格避开忌口/过敏，并贴合热量参考与健康目标。
- **快捷记一餐**：已吃过饭时不必走推荐流程，选定食材后可让 AI 起名或自己填，直接记账。
- **数据备份**：导出为文件 / 复制内容 / 合并导入 / 覆盖导入 / 清空本机数据；
  用于换机迁移，或把自己的配置（含 API Key）一次导给家人。
- **设置页**：API Key、数据备份、版本三个分区；版本区显示 App 版本与构建时间，
  并提供「强制刷新到最新版」。

### 变更

- **纯前端化**：移除全部服务端 API 路由，改为浏览器直连 DeepSeek
  （文本用 `deepseek-chat`，截图识别用 V4.1 Flash 的视觉能力 `deepseek-flash`）。
  因此可完整静态导出，无服务器、零成本，数据只存本地。
- **手机独立使用**：支持「添加到主屏幕」当 App 用；iOS 侧做了安全区适配、
  4 种机型启动图、`storage.persist()` 申请持久存储、安装引导横幅。
- **更新机制**：Service Worker 缓存版本化、HTML 强制验证（绕过 HTTP 缓存）、
  `updateViaCache: "none"`、回到 App 自动检查更新、发现新版本时页面顶部提示。
  更新不影响本地数据。

### 修复

- 修复子路径部署下站内跳转 404（原生 `<a>` 不会自动加 basePath）。
- 修复健康档案的年龄/身高/体重**删不掉、总留个 0**（受控数字输入把空串读成 0）。
- 修复截图导入时**不识别店铺名**，导致所有菜都归到「学校食堂」。
- 修复 Service Worker 的 `cache.addAll` 静默失败（任一 404 会让整个离线缓存为空）。
- 修复 iOS 主屏 App 更新后看不到新版、以及点「立即更新」无反应的问题。
- 修复「发现新版本」横幅在首次安装时误报。

### 部署

- 新增 `部署.bat`：一键完成构建（含子路径）、注入版本号、发布到 GitHub Pages。
- 新增 `scripts/verify-subpath.py`：解析页面里的真实链接逐个访问，
  等价于把每个按钮点一遍，用于补上「只测路由地址测不出点击 404」的漏洞。

### 说明

- 本项目是上游项目的衍生版本。上游**未附 LICENSE、未声明开源协议**（默认保留所有权利），
  故本衍生版仅供个人自用，不应公开分发。详见 `README.md` 顶部声明。
